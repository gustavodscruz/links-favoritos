<!doctype html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Gerenciar Categorias - Links Favoritos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <style>
        :root {
            --bs-primary: #6d28d9;
            --bs-primary-rgb: 109, 40, 217;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(135deg, rgba(109, 40, 217, 0.08), rgba(99, 102, 241, 0.05));
            font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
        }

        .navbar {
            background: #fff !important;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
        }

        .navbar-brand {
            color: var(--bs-primary) !important;
            font-weight: 600;
        }

        .page-header {
            background: linear-gradient(135deg, rgba(109, 28, 166, 0.95), rgba(109, 40, 217, 0.92));
            padding: 24px;
            border-radius: 12px;
            margin-bottom: 24px;
            color: #fff;
            box-shadow: 0 4px 20px rgba(109, 40, 217, 0.25);
        }

        .page-header h3 {
            color: #fff;
            font-weight: 600;
            margin: 0;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
        }

        .card-title {
            color: var(--bs-primary);
            font-weight: 600;
            margin-bottom: 16px;
        }

        .category-table th {
            background: linear-gradient(90deg, rgba(var(--bs-primary-rgb), 0.08), rgba(99, 102, 241, 0.04));
            color: var(--bs-primary);
            font-weight: 600;
        }

        .btn-edit {
            color: #fff;
            background-color: #10b981;
            border: none;
        }

        .btn-edit:hover {
            background-color: #059669;
        }

        .btn-delete {
            color: #fff;
            background-color: #ef4444;
            border: none;
        }

        .btn-delete:hover {
            background-color: #dc2626;
        }

        .btn-primary {
            background-color: var(--bs-primary);
            border-color: var(--bs-primary);
        }

        .btn-primary:hover {
            background-color: #5b21b6;
            border-color: #5b21b6;
        }

        .btn-outline-primary {
            color: var(--bs-primary);
            border-color: var(--bs-primary);
        }

        .btn-outline-primary:hover {
            background-color: var(--bs-primary);
            border-color: var(--bs-primary);
        }

        .form-card {
            background: #f8f9fb;
            padding: 20px;
            border-radius: 10px;
        }

        .table-actions button {
            margin-right: 6px;
        }

        .small-note {
            font-size: 0.875rem;
            color: #6b7280;
        }
    </style>
</head>

<body>
    {{> partials/header}}

    <div class="container mt-4 mb-5">
        <div class="page-header">
            <div class="d-flex justify-content-between align-items-center">
                <h3>📂 Gerenciar Categorias</h3>
                <a href="/dashboard" class="btn btn-light">← Voltar ao Dashboard</a>
            </div>
        </div>

        {{#success}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            ✅ Categoria cadastrada com sucesso!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/success}}
        {{#deleteSuccess}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            ✅ Categoria excluída com sucesso!
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/deleteSuccess}}
        {{#deleteError}}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            ❌ Erro ao excluir a categoria.
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/deleteError}} <div class="row g-4">
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">📋 Categorias Existentes</h5>
                        <div class="table-responsive">
                            <table class="table table-hover category-table align-middle">
                                <thead>
                                    <tr>
                                        <th style="width: 60px">#</th>
                                        <th>Nome</th>
                                        <th style="width: 180px">Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#categorias}}
                                    <tr>
                                        <td class="text-muted">{{id}}</td>
                                        <td><strong>{{name}}</strong></td>
                                        <td class="table-actions">
                                            <form action="/categoria/edit" method="get" style="display:inline">
                                                <input type="hidden" name="id" value="{{id}}">
                                                <button class="btn btn-sm btn-edit" type="submit">✏️ Editar</button>
                                            </form>
                                            <form action="/categoria/delete" method="post" style="display:inline">
                                                <input type="hidden" name="id" value="{{id}}">
                                                {{> partials/csrf}}

                                                <button class="btn btn-sm btn-delete" type="submit"
                                                    onclick="return confirm('Confirma a exclusão da categoria {{name}}?')">🗑️
                                                    Deletar</button>
                                            </form>
                                        </td>
                                    </tr>
                                    {{/categorias}}
                                    {{^categorias}}
                                    <tr>
                                        <td colspan="3" class="text-center text-muted py-4">
                                            📭 Nenhuma categoria encontrada. Crie sua primeira categoria ao lado!
                                        </td>
                                    </tr>
                                    {{/categorias}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">➕ Nova Categoria</h5>
                        <form action="/categoria/add" method="post">
                            {{> partials/csrf}}


                            <div class="mb-3">
                                <label for="name" class="form-label">Nome da categoria</label>
                                <input type="text" name="name" id="name" class="form-control"
                                    placeholder="Ex: Programação, Receitas..." required autofocus>
                                <div class="form-text">Escolha um nome descritivo para sua categoria.</div>
                            </div>

                            <div class="d-grid">
                                <button class="btn btn-primary btn-lg" type="submit">💾 Salvar Categoria</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
</body>

</html>